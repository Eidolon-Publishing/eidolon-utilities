// Legacy idle-animation hooks — disabled.
// All animation lifecycle is now handled by tile-interactions/hooks.js
// using the unified tile-animations flag with migration fallback.

export function registerIdleAnimationHooks() {
	// No-op: hooks are now registered in tile-interactions/hooks.js
}
