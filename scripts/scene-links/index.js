/**
 * Scene-links feature entry point.
 *
 * Registers hooks for scene linking, bidirectional sync, and SceneConfig tab.
 */

import { registerSceneLinksHooks } from "./hooks.js";

registerSceneLinksHooks();
